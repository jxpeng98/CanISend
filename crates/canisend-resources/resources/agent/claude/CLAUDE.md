# CanISend Agent Protocol v2 for Claude

Inspect `canisend agent capabilities --json` before starting. Complete bounded tasks through JSON candidates and do
not write `.canisend/` internal state. Follow the descriptor's input revisions, output schema, privacy scope, and
required consents. Readiness means material preparation status, never application submission.
