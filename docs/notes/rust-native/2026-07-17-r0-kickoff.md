# R0 Kickoff Note

**Date:** 2026-07-17

**Branch:** `rewrite/rust-native`

**Roadmap:** `docs/superpowers/plans/2026-07-17-rust-native-greenfield-roadmap.md`

## Decision recorded

The rebuild uses the existing repository and Git history, but replaces the active product with a new Rust-native
implementation. It is not a line-by-line port and does not maintain a Python compatibility layer.

The final active Python state was clean before the roadmap file was introduced. It is preserved by the annotated tag
`archive/python-v0.6.0b1-final` at commit `18ab40815fa7976e2ce4ab9ef91e3f4826689af3`.

## Immediate sequence

1. Commit the approved roadmap and archive documentation.
2. Add the eight Rust-native architecture decision records.
3. Add synthetic v2 executable specifications and dependency spike evidence.
4. Mark R0 complete only when its exit criteria are evidenced.
5. Remove the active Python project during R1, after the archive and R0 decisions are committed.

## Tracking rules

- Update the roadmap checklist in the same commit that satisfies a task when practical.
- Add a dated note when a phase starts, a dependency choice is made, a release gate changes, or a material risk is
  discovered.
- Keep commits bounded to one auditable milestone.
- Do not mark a phase complete from intent; record tests, command output, or reviewed artifacts that prove its exit
  criteria.
