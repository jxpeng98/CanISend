{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "CriteriaCheck",
  "description": "Structured content contract for criteria checklist rows. Current CanISend CLI outputs 05_criteria_checklist.md as Markdown; this schema is for future JSON/structured exports or parsed row-equivalent validation, not direct Markdown-file validation.",
  "type": "object",
  "required": ["criteria"],
  "properties": {
    "criteria": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["criterion", "coverage", "evidence_source", "risk", "suggested_improvement"],
        "properties": {
          "criterion": {"type": "string"},
          "coverage": {"enum": ["strong", "partial", "weak", "missing"]},
          "evidence_source": {"type": "string"},
          "risk": {"enum": ["Low", "Medium", "High"]},
          "suggested_improvement": {"type": "string"}
        }
      }
    }
  }
}
