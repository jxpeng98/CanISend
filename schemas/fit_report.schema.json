{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "FitReport",
  "description": "Structured content contract for future JSON fit report exports. Current CanISend CLI outputs 02_fit_report.md as Markdown, so this schema does not directly validate the current Markdown artifact.",
  "type": "object",
  "required": ["summary", "strengths", "gaps", "risks"],
  "properties": {
    "summary": {"type": "string"},
    "strengths": {"type": "array", "items": {"type": "string"}},
    "gaps": {"type": "array", "items": {"type": "string"}},
    "risks": {"type": "array", "items": {"type": "string"}}
  }
}
